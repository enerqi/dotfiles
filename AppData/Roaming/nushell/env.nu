# Nushell Environment Config File
#

# Minimal env.nu — mostly for PATH and important env vars

# Example: Add common paths
# $env.PATH = ($env.PATH | append [
#     "/usr/local/bin"
#     ($env.HOME | path join ".local" "bin")
# ])

# Tools like mise, fnm, etc. can generate their own autoload files
